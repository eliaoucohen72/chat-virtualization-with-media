import { defineMessages } from 'react-intl';

export default defineMessages({
  parentGroupMessage: {
    id: 'app.components.Chat.parentGroupMessage.message',
    defaultMessage: 'Chat is unavailable on multi select state'
  }
});
